<div class="col-md-6">
    <div class="card mb-4">
        <div class="card-header">
            <h3 class="card-title">Nominal</h3>
        </div> <!-- /.card-header -->
        <div class="card-body p-0">
            <table class="table">
                <thead>
                    <tr>
                        <th style="width: 10px">No</th>
                        <th>Name</th>
                        <th>Quantity</th>
                        <th>Price</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let i = 0; i < nominal.length; i++){ %>
                    <tr class="align-middle">
                        <td><%= i+1 %></td>
                        <td><%= nominal[i].name %></td>
                        <td><%= nominal[i].quantity %></td>
                        <td><%= nominal[i].price %></td>
                        <td>
                            <form action="/nominal/<%= nominal[i]._id %>?_method=DELETE" method="POST">
                                <a class="btn btn-warning" href="/nominal/update/<%= nominal[i]._id %>">Update</a>
                                <button class="btn btn-danger">Remove</button>
                            </form>
                        </td>
                    </tr>
                    <% } %>
                </tbody>
            </table>
        </div> <!-- /.card-body -->
    </div> <!-- /.card -->
</div>